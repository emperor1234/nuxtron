# Test/dev dependencies for the Nuxtron FastAPI service.
-r requirements.txt
pytest==9.0.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1
# starlette>=1.0's TestClient (used by fastapi.testclient.TestClient, which
# 165 test files import) requires httpx2 specifically — httpx alone is no
# longer sufficient. See starlette.testclient's own RuntimeError if missing.
httpx2==2.9.1
aiosqlite==0.20.0
greenlet==3.1.1
ruff==0.8.4
mypy==1.13.0
bandit==1.8.0
pip-audit==2.7.3
hypothesis==6.142.4
import-linter==2.5
semgrep==1.172.0
